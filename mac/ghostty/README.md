# Ghostty setting

## Setup

メインの`setup.sh`を実行すれば自動的にリンクされます。

手動でセットアップする場合：

```bash
trash "$HOME/Library/Application Support/com.mitchellh.ghostty/config"
ln -s ~/dotfiles/mac/ghostty/config "$HOME/Library/Application Support/com.mitchellh.ghostty/config"
```

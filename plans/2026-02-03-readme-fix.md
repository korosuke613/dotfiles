# dotfiles-sync.sh README 修正計画

## 概要
README.dotfiles-sync.md の起動条件の記載を、実装に合わせて修正する。

## 修正内容

### 対象ファイル
- `mac/scripts/README.dotfiles-sync.md`

### 変更箇所
**23行目**を修正:

| Before | After |
|--------|-------|
| `- stdout が TTY のときのみ（-t 1）` | `- stdin と stderr が TTY のときのみ（-t 0, -t 2）` |

## 修正理由
スクリプトの実装（74行目）では以下のチェックを行っている:
```bash
if [[ ! -t 0 ]] || [[ ! -t 2 ]] || ...
```

- `-t 0`: stdin が TTY かどうか（ユーザーが入力できる状態）
- `-t 2`: stderr が TTY かどうか（エラーメッセージが見える状態）

この実装は「人間が対話的に操作できる環境」を正しく検出している。

## 検証方法
1. 修正後、README を読み直して記載が実装と一致していることを確認
2. スクリプトの動作に影響はないため、動作テストは不要
